# SX9-PROMPT v4.0
# Generated: 2025-12-23T07:34:46.889356Z

header:
  title: "GLAF Graph Annotation (Sub-Agent)"
  rfc: RFC-9112
  phase: IMPLEMENT

utilization:
  harness: Build & Implement
  persona: FORGE
  mode: SUPERVISED

mission:
  objective: >
    Emit GLAF graph delta updates for canonical matches and findings. No code changes. Output graph.delta.json only.

constraints:
  code_write: false
  output_format: "json"
  determinism:
    temperature: 0.0
    top_p: 1.0
  safety:
    no_speculation: true
    no_prose: true

io:
  inputs:
    - repo_path
    - canonical_registry_path
    - static_outputs_path
    - semantic_outputs_path
  outputs:
    - graph.delta.json

success_criteria:
  - "No code modifications performed"
  - "Outputs validate against schemas"
  - "All findings include evidence"
